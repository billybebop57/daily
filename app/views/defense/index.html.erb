<h1>Defense</h1>
<h2>Optimal Cash Plays</h2>
	<div class='optimal'>
		<% @defense.optimal.each do |qb| %>
			<%= "#{qb[:name]}\t(#{qb[:avg_proj].to_f.round(1)} pts)" %><br>
		<% end %>
	</div>

<div class="pure-g">
	    <div class="pure-u-1-2">
	    	<h2>Top 5 Projected</h2>
			<% @defense.top_projected.each do |qb| %>
				<%= "#{qb[:name]}\t(#{qb[:avg_proj].to_f.round(1)} pts)" %><br>
			<% end %>
	    </div>
	    <div class="pure-u-1-2">
	    	<h2>Top Value</h2>
			<% @defense.top_value.each do |qb| %>
				<%= "#{qb[:name]}\t(#{qb[:avg_proj].to_f.round(1)} pts)" %><br>
			<% end %>
	    </div>
	</div>